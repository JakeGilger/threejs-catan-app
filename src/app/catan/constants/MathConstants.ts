export class MathConstants {
  static readonly SQRT_3 = Math.sqrt(3);
  static readonly TWO_PI = Math.PI * 2;

  // ROTATION CONSTANTS

  // Quarter turn clockwise rotation
  static readonly NEG_PI_OVER_2: number = -1 * Math.PI / 2;
  // Third turn clockwise rotation
  static readonly NEG_PI_OVER_3 = -1 * Math.PI / 3;
  // Third turn counter-clockwise rotation
  static readonly PI_OVER_3 = Math.PI / 3;
}
